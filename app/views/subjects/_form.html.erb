
<%= simple_form_for @subject do |f| %>
	<%= f.input :name, label: 'Name:' %>
	<%= f.input :lecturer, label: 'Lecturer:' %>
	<%= f.input :subject_code, label: 'Subject code:' %>
	<%= f.button :submit %>
	<% end %>